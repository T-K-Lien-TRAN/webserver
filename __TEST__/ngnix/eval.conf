server {
	listen 8000;
	server_name localhost;
	root /www;
}

server {
	listen 8000;
	server_name webserver;
	root /www;
}

server {
	listen 8030;
	server_name localhost;
	root /www;

	allow_met
}

server {
	listen 8030;
	server_name webserver;
	root /www;
}